using System;
using System.Drawing;
using System.Reflection;

namespace CherryTomato
{
    public static class Helpers
    {
        //public static Icon LoadIcon(string path)
        //{
        //    if (path.StartsWith("res://"))
        //    {
        //        var resourceName = path.Substring("res://".Length);
        //        return new Icon(Assembly.GetCallingAssembly().GetManifestResourceStream("CherryTomato.Resources." + resourceName));
        //    }

        //    throw new Exception("Currently, only resource icons are supported currently (path='" + path + "')");
        //}
    }
}
